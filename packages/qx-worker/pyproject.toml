[project]
name = "qx-worker"
version = "1.1.0"
description = "Qx worker runtime: NATS consumer with retry/DLQ/tracing"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
authors = [{ name = "Qx Engineering" }]
dependencies = [
    "qx-core",
    "qx-di",
    "qx-cqrs",
    "qx-db",
    "qx-events",
    "qx-observability",
    "nats-py>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qx"]
