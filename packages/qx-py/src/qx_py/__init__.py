"""qx-py — full-stack Qx framework meta-package.

Installing this package pulls in all qx sub-packages:

    pip install qx-py

Individual sub-packages remain independently installable for
services that only need a subset of the framework.
"""

__version__ = "0.2.0"
