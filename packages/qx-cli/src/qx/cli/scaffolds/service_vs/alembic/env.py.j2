"""Alembic env — defers to the framework helper.

Shared MetaData is assembled from the service's ``shared`` module; each
slice registers its Table definitions against it.
"""

from __future__ import annotations

from qx.db.migrations import run_async_migrations

from {{ service_pkg }}.shared import metadata

run_async_migrations(metadata)
