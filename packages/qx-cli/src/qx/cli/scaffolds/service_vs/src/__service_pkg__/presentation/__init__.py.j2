"""{{ service_pascal }} presentation layer.

One FastAPI router per slice, e.g. {{ domain_snake }}.py.
Mount each router in main.py:
    app.include_router({{ domain_snake }}_router, prefix="/v1")
"""
