"""Delete{{ slice_name_pascal }} command + handler."""

from __future__ import annotations

from uuid import UUID

from qx.core import Result
from qx.cqrs import Command, command_handler
from qx.db import UnitOfWork

from {{ service_pkg }}.infrastructure.persistence.{{ slice_name_snake }}.repository import (
    {{ slice_name_pascal }}Repository,
)


class Delete{{ slice_name_pascal }}Command(Command[None]):
    """Soft-delete a {{ slice_name_pascal }} by ID."""

    id: UUID


@command_handler(Delete{{ slice_name_pascal }}Command)
class Delete{{ slice_name_pascal }}Handler:
    def __init__(self, uow: UnitOfWork) -> None:
        self._uow = uow

    async def handle(self, command: Delete{{ slice_name_pascal }}Command) -> Result[None]:
        async with self._uow:
            repo = {{ slice_name_pascal }}Repository(self._uow.session)
            result = await repo.soft_delete(command.id)
            if result.is_failure:
                return result
            await self._uow.commit()
        return Result.success(None)
